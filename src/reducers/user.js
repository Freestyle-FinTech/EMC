const initialState = {
  userName: "Ryan Gary",
  userId: "4x3sf4g5w2r4ad",
}

const user = (state = initialState, action) => {
  // switch (action.type) {
  //   case SET_USER_NAME:
  //     return {...state, userName: action.payload}
  //   default:
    // return state
  // }
  return state
}

export default user